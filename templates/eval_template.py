"""
评估脚本模板 — 用于 evaluator.py 生成最终评估脚本时参考。

实际生成由 core/evaluator.py 的 build_eval_script() 方法完成。
"""

# 模板变量说明：
# {{model_path}}  — 基座模型路径
# {{lora_path}}   — LoRA adapter 路径
# {{test_data}}   — 测试数据路径
